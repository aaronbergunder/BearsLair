<%= form_for(@skillsheet) do |f| %>
  <% if @skillsheet.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@skillsheet.errors.count, "error") %> prohibited this skillsheet from being saved:</h2>

      <ul>
      <% @skillsheet.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :character_id %><br>
    <%= f.text_field :character_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
